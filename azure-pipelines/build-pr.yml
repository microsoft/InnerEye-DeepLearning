pr:
  branches:
    include:
    - '*'

name: PR-$(Date:yyyyMMdd)$(Rev:-r)

jobs:
  - job: CleanupAML
    pool:
      vmImage: 'ubuntu-18.04'
    steps:
      - template: cancel_aml_jobs.yml

  - job: RunAllTests
    depends_on: CleanupAML
    steps:
      - template: run_all_tests.yml
